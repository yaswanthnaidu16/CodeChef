for _ in range(int(input())):
    n,p,x,y = map(int,input().split())
    a = list(map(int,input().split()))
    count = 0
    for i in range(p):
        if a[i] == 0:
            count += x
        else:
            count += y
    print(count)
