#Use sliding window for optimization of time complexity

for _ in range(int(input())):
    n, k = map(int, input().split())
    a = list(map(int, input().split()))

    window_sum = sum(a[:k])
    max_sum = window_sum

    for i in range(k, n):
        window_sum += a[i] - a[i-k]
        max_sum = max(max_sum, window_sum)

    print(max_sum)
