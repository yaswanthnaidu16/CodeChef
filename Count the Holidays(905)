for _ in range(int(input())):
    n=int(input())
    a=list(map(int,input().split()))
    s=set(a)
    for i in range(6,31,7):
        s.add(i)
    for i in range(7,31,7):
        s.add(i)
    print(len(s))
